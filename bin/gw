#!/usr/bin/env bash
# gw -- short wrapper for gitwise. See bin/gitwise for Python resolution logic.
set -Eeuo pipefail
_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd -P)"
exec "$_dir/gitwise" "$@"
