name: CI

on:
  pull_request:
  push:
    branches: [main]
  workflow_call:

concurrency:
  group: ci-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  docs:
    name: Docs Consistency
    if: >
      ${{ github.event_name != 'push' ||
          !startsWith(github.event.head_commit.message || '', 'docs(changelog): sync CHANGELOG.es from CHANGELOG') }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # v8.2.0
        with:
          enable-cache: false
      - run: uv sync --group dev
      - run: uv run python scripts/docs/check_i18n_pairs.py
      - run: uv run python scripts/docs/check_md_links.py
      - run: uv run python scripts/docs/check_roadmap_baseline.py
      - run: uv run python scripts/docs/check_schema_catalog.py

  workflow-audit:
    name: Workflow Audit
    if: >
      ${{ github.event_name != 'push' ||
          !startsWith(github.event.head_commit.message || '', 'docs(changelog): sync CHANGELOG.es from CHANGELOG') }}
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # v8.2.0
        with:
          enable-cache: false
      # PyYAML is a one-off tool dependency; do not add it to the project.
      - run: uv run --with pyyaml python scripts/audit-template-injection.py

  lint:
    name: Lint & Format
    if: >
      ${{ github.event_name != 'push' ||
          !startsWith(github.event.head_commit.message || '', 'docs(changelog): sync CHANGELOG.es from CHANGELOG') }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # v8.2.0
        with:
          enable-cache: false
      - run: uv sync --group dev
      - run: uv run ruff check gitwise/ tests/
      - run: uv run ruff format --check gitwise/ tests/

  typecheck:
    name: Type Check
    if: >
      ${{ github.event_name != 'push' ||
          !startsWith(github.event.head_commit.message || '', 'docs(changelog): sync CHANGELOG.es from CHANGELOG') }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # v8.2.0
        with:
          enable-cache: false
      - run: uv sync --group dev
      - run: uv run basedpyright

  test:
    name: Tests (${{ matrix.os }}, ${{ matrix.python-version }})
    if: >
      ${{ github.event_name != 'push' ||
          !startsWith(github.event.head_commit.message || '', 'docs(changelog): sync CHANGELOG.es from CHANGELOG') }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    needs: [docs, lint, typecheck]
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # v8.2.0
        with:
          python-version: ${{ matrix.python-version }}
      - run: uv sync --group dev
      - run: uv run pytest -q --tb=short -n 2 --cov=gitwise --cov-report=term-missing --cov-report=xml
      - name: Upload coverage to Codecov
        if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.12'
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f # v7.0.0
        with:
          files: ./coverage.xml
          fail_ci_if_error: false

  shellcheck:
    name: Shell Lint
    if: >
      ${{ github.event_name != 'push' ||
          !startsWith(github.event.head_commit.message || '', 'docs(changelog): sync CHANGELOG.es from CHANGELOG') }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - name: shellcheck
        run: shellcheck install.sh bin/gitwise

  audit:
    name: Dependency Audit
    if: >
      ${{ github.event_name != 'push' ||
          !startsWith(github.event.head_commit.message || '', 'docs(changelog): sync CHANGELOG.es from CHANGELOG') }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # v8.2.0
        with:
          enable-cache: false
      - run: uv sync --group dev
      - run: uv run pip-audit
