{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://gitwise.dev/schemas/v1/input/diff.json",
  "title": "gitwise diff cli input",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "lang": {
      "type": "string",
      "enum": [
        "es",
        "en"
      ],
      "description": "output language (default: auto-detect from locale)"
    },
    "theme": {
      "type": "string",
      "enum": [
        "dark",
        "light",
        "auto"
      ],
      "description": "color theme: dark, light, or auto-detect (default: auto)"
    },
    "json": {
      "type": "boolean",
      "description": "output JSON",
      "default": false
    },
    "json_pretty": {
      "type": "boolean",
      "description": "pretty-print JSON output",
      "default": false
    },
    "staged": {
      "type": "boolean",
      "description": "show staged changes only",
      "default": false
    },
    "name_only": {
      "type": "boolean",
      "description": "show only file names",
      "default": false
    },
    "full": {
      "type": "boolean",
      "description": "show full patch with delta integration",
      "default": false
    },
    "summary": {
      "type": "boolean",
      "description": "compact summary: additions/deletions per file, no patch",
      "default": false
    },
    "stat": {
      "type": "boolean",
      "description": "show diffstat (default behavior)",
      "default": false
    },
    "scan_secrets": {
      "type": "boolean",
      "description": "scan the diff for leaked credentials (advisory, opt-in)",
      "default": false
    },
    "json_lines": {
      "type": "boolean",
      "description": "stream one JSON envelope per changed file (NDJSON)",
      "default": false
    },
    "refspec": {
      "type": "string",
      "description": "commit, branch, or range to diff (e.g. HEAD~3, main..feat, main...feat)"
    },
    "paths": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "limit to paths (use -- to separate from refspec)"
      }
    },
    "git_args": {
      "type": "string",
      "description": "forward an extra option to the underlying git diff (repeatable; code-exec/write/redirect options like --output/-c/--git-dir are refused)"
    }
  }
}
